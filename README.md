# Hey there! 👋
Here I share a NodeJS project with JWT JSON Web token Authentication with JavaScript

# NodeJS project with JWT JSON Web token Authentication with JavaScript


## Requirements
*  Nodejs
    ```bash
    npm init -y
    ```
*  JWT library
    ```bash
    npm install jsonwebtoken
    ```
To execute the app in this repository

1.  Navigate to the directory containing the  files in your terminal.
2.  Run the desired script in the terminal:
    ```bash
    node jwt_demo.js
    ```

    
